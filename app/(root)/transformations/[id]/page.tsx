import React from "react";

type Props = {
  params: { id: string };
};

const TransformationsPage = ({ params: { id } }: Props) => {
  return <div>Trans</div>;
};

export default TransformationsPage;
